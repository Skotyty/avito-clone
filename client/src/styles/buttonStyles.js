import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

// Кнопка "К объявлениям"
export const StyledBackButton = styled(Button)({
  marginTop: "20px",
  width: "200px",
  fontSize: "16px",
  fontWeight: "bold",
  backgroundColor: "blue"
});
